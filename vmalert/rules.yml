groups:
  - name: store-api
    rules:
      - alert: HighErrorRate
        expr: go_app_http_requests_total{status_code=~"5.."} > 50
        for: 1m
        labels:
          severity: critical
          owner_team: my_team
        annotations:
          summary: High error rates detected from store api
