groups:
  - name: rideshare-app
    rules:
      # - alert: HighErrorRate
      #   expr: |
      #     INSERT ERROR RATE QUERY HERE
      #     > 0.25
      #   for: 10s
      #   labels:
      #     severity: critical
      #     owner_team: my_team
      #   annotations:
      #     summary: High error rates detected from rideshare app
